package utils;

import models.JSONData;

public class JSONUtils {
    public static JSONData parse(String jsonData) {
        JSONData parsedJSONData = new JSONData();
        return parsedJSONData;
    }
}
